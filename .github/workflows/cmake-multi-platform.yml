name: CMake multi-platform build + vcpkg + CPack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false

      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        c_compiler: [gcc, clang, cl]

        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl

          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++

          - os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++

        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl

    steps:
    - name: Checkout
      uses: actions/checkout@v4
          
    # =========================
    # MSVC ENV (CRITICAL FIX)
    # =========================
    - name: Setup MSVC (Windows only)
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64

    # =========================
    # Linux deps
    # =========================
    - name: Install Linux deps
      if: runner.os == 'Linux'
      run: sudo apt-get update && sudo apt-get install -y ninja-build autoconf automake libtool pkg-config libudev-dev

    # =========================
    # Ninja (Windows)
    # =========================
    - name: Install Ninja (Windows)
      if: runner.os == 'Windows'
      run: choco install ninja -y

    # =========================
    # vcpkg
    # =========================
    - name: Clone vcpkg
      run: git clone https://github.com/microsoft/vcpkg.git

    - name: Checkout vcpkg version
      working-directory: vcpkg
      run: git checkout 2025.04.09

    - name: Bootstrap vcpkg (Linux)
      if: runner.os != 'Windows'
      run: ./vcpkg/bootstrap-vcpkg.sh

    - name: Bootstrap vcpkg (Windows)
      if: runner.os == 'Windows'
      run: .\vcpkg\bootstrap-vcpkg.bat

    - name: Set VCPKG_ROOT
      shell: bash
      run: echo "VCPKG_ROOT=${{ github.workspace }}/vcpkg" >> $GITHUB_ENV

    # =========================
    # Cache vcpkg
    # =========================
    - name: Cache vcpkg
      uses: actions/cache@v4
      with:
        path: |
          ${{ env.VCPKG_ROOT }}/installed
          ${{ env.VCPKG_ROOT }}/packages
          ~/.cache/vcpkg
        key: ${{ runner.os }}-vcpkg-2025.04.09-${{ hashFiles('vcpkg.json') }}

    # =========================
    # Build directory
    # =========================
    - name: Set build dir
      id: strings
      shell: bash
      run: echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    # =========================
    # CONFIGURE (NINJA FIXED)
    # =========================
    - name: Configure CMake
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -G Ninja
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DCMAKE_TOOLCHAIN_FILE=${{ env.VCPKG_ROOT }}/scripts/buildsystems/vcpkg.cmake
        -S ${{ github.workspace }}

    # =========================
    # BUILD
    # =========================
    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }}

    # =========================
    # TEST
    # =========================
    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: ctest --build-config ${{ matrix.build_type }}

    # =========================
    # PACKAGE (CPACK)
    # =========================
    - name: Package
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: cpack -C ${{ matrix.build_type }}

    # =========================
    # UPLOAD ARTIFACTS
    # =========================
    - name: Upload package
      uses: actions/upload-artifact@v4
      with:
        name: ${{ runner.os }}-${{ matrix.build_type }}
        path: ${{ steps.strings.outputs.build-output-dir }}/*
